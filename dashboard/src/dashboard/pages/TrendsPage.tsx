import { PageScaffold } from '../components/PageScaffold';

export function TrendsPage() {
  return (
    <PageScaffold
      title="Weight and adherence trends"
      body="Sparkline and trend history panel is present and follows the dashboard mockup structure for this initial presentational session."
    />
  );
}
