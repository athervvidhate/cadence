# Preferred on iOS: on-device Zetic model config
# NOTE: This key is embedded in your dev build; use a non-production key for demos.
EXPO_PUBLIC_ZETIC_PERSONAL_KEY=your_melange_personal_key
EXPO_PUBLIC_ZETIC_MODEL_ID=Steve/text-anonymizer-v1
EXPO_PUBLIC_ZETIC_MODEL_VERSION=1

# Optional fallback: backend Zetic anonymization endpoint (POST JSON with "text")
EXPO_PUBLIC_ZETIC_ANONYMIZE_URL=https://your-backend.example.com/zetic/anonymize

# Gemma (demo backend): POST JSON { "anonymizedText", "task": "discharge-medication-assistant" }
# Use your Mac's LAN IP so the phone can reach the machine running `npm run dev` in backend/
# Example: EXPO_PUBLIC_LLM_PIPELINE_URL=http://192.168.1.12:8787/llm/medication-summary
EXPO_PUBLIC_LLM_PIPELINE_URL=https://your-backend.example.com/llm/medication-summary

# Optional: same host as LLM, POST JSON { "text" } — e.g. http://192.168.1.12:8787/tts/elevenlabs
EXPO_PUBLIC_ELEVENLABS_TTS_URL=https://your-backend.example.com/tts/elevenlabs
